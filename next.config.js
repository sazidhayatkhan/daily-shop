/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'], // Add any other domains you want to allow here
      },
}

module.exports = nextConfig